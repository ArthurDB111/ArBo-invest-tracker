services:
  - type: web
    name: arbo-invest-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: FMP_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        value: "*"
